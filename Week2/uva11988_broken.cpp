#include <iostream>
#include <string>
#include <list>
using namespace std;

string check_beiju(string &text){
    if (text.empty()){
        return;
    }
    

    return text;
}

int main(){
    string text;
    cin >> text;

    text = check_beiju(text);
    cout << text;
}